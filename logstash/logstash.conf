input {
  tcp {
    port  => 5044
    codec => json_lines
  }
}

filter {
  # Remove Logstash-internal bookkeeping field
  mutate {
    remove_field => ["@version"]
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "lms-logs-%{+YYYY.MM.dd}"
  }
}
